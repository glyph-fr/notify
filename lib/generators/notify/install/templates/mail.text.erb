Hey <%= @recipient.name %>, you have <%= pluralize(@email.notifications_count, "unread notification") %> !

<% @notifications.each do |notification| %>
<%= render partial: "notify/notifications_digests_mailer/notifications/#{ notification.partial_key }", locals: { notification: notification } %>
<% end %>


-------------------------------------------------------------------------
This is an automatic message, please don't try to send an answer
